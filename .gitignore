# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# playwright / local scratch
.playwright-mcp/
playwright-report/
test-results/
grants-*.jpeg
grants-dom.md
admin-modules*.png
audit-report/
synthetixvideo-audit-report.md

# self-improvement daemon runtime state/output (regenerated on every cycle)
scripts/.live-audit-state.json
scripts/.local-audit-report.json
scripts/self-eval-metrics.jsonl
scripts/improvement-queue.md
scripts/self-criticism-findings.md
scripts/.gpu.lock
scripts/.pids/
scripts/.supervisor.pid
scripts/.watchdog-state.json

# Environment files - never commit (secrets). Use .env.example as the template.
.env
.env.*
!.env.example

# git filter-branch's own scratch directory — accidentally committed once by
# the auto-sync script's `git add -A` while a prior filter-branch run was
# mid-flight and left this behind in the working tree.
.git-rewrite/

# Open-data download cache. scripts/import-cra-t3010.ts re-downloads any file
# that is missing, so these 25 MB of CSVs were pure repo weight.
data/cra-t3010/

# Generated audit/report output — regenerated by the scripts that write them.
scripts/local-audit-report*.json
scripts/data-quality-analysis.md
scripts/extraction-validator-report.md

# Runtime scratch and stray captures that were committed by the auto-sync
# watcher's `git add -A` rather than deliberately.
dev_pid.txt
/auth-page.png
/fit-rules.png

# Design review archive kept locally, not repo content.
Desing/
